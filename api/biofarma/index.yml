swagger: '2.0'
info:
  version: '1.0.0'
  title: Test Biofarma
schemes: [http]
consumes: [application/json, multipart/form-data]
produces: [application/json]
securityDefinitions:
  authorization:
    type: apiKey
    in: header
    name: Authorization

paths:
  # health
  /health:
    $ref: ./path/v1/health/health.yml

  # home
  /v1/home:
    $ref: ./path/v1/home/home.yml

definitions:
  Home:
    allOf:
      - $ref: "./../common/definitions/common.yml#/definitions/ModelIdentifier"
      - $ref: "./../common/definitions/common.yml#/definitions/ModelTrackTime"
      - $ref: "./definitions/home.yml#/definitions/HomeData"